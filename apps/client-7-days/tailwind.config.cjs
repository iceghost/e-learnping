const config = {
	content: ['./src/**/*.{html,js,svelte,ts}'],

	theme: {
		extend: {
			fontFamily: {
				sans: ['Be Vietnam Pro', 'sans-serif']
			}
		}
	},

	plugins: []
};

module.exports = config;
